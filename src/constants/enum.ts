export enum Roles {
  USER,
  CREATOR
}
export enum Levels {
  INTERN="INTERN",
  FRESHER="FRESHER",
  JUNIOR="JUNIOR",
  MIDDLE="MIDDLE",
  SENIOR="SENIOR",
}
export enum TokenTypes {
  AccessToken,
  RefreshToken,
}
export enum JobStatus {
  PENDING="PENDING",
  APPROVED="APPROVED",
  REJECTED="REJECTED",
}
export enum JobTypes {
  FULLTIME="FULLTIME",
  PARTTIME="PARTTIME",
}