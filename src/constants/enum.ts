export enum Roles {
  USER,
  CREATOR
}
export enum Levels {
  INTERN="INTERN",
  FRESHER="FRESHER",
  JUNIOR="JUNIOR",
  MIDDLE="MIDDLE",
  SENIOR="SENIOR",
}
export enum TokenTypes {
  AccessToken,
  RefreshToken,
}
export enum JobStatus {
  PENDING,
  APPROVED,
  REJECTED,
}
export enum JobTypes {
  FULLTIME="FULLTIME",
  PARTTIME="PARTTIME",
}